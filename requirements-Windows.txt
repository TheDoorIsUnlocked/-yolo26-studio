# =============================================================
# YOLO26 AI 视觉 Studio —— Windows x64 依赖清单
# 用法见《部署教程_Windows_x64.md》
#
# 说明：
#   1. 本项目根目录自带本地 ultralytics/ 源码(8.4.132)，运行时会
#      优先使用本地这份；本文件里的包只是它的「运行依赖」，不需要
#      额外 pip install ultralytics（装了也会被本地源码覆盖）。
#   2. torch / torchvision 需要 CUDA 12.6 的专用 wheel，不能走普通
#      清华/阿里 pypi（那里只有 CPU 版），请用单独的命令单独安装
#      （见教程第 5.1 节），不要写进这个文件。
#   3. 下面的版本号取自开发机已验证可运行的环境(python 3.12.12)。
#      直接用 pip install -r 即可，pip 会自动拉齐其余传递依赖。
# =============================================================

# ---------- 图形界面 ----------
PyQt6==6.10.2

# ---------- 图像处理 / 屏幕采集 ----------
opencv-python==4.12.0.88
mss==10.2.0
Pillow==12.1.0

# ---------- 科学计算 / 数据 ----------
numpy==2.1.3
scipy==1.17.0
matplotlib==3.10.8
PyYAML==6.0.3
psutil==7.2.1
requests==2.32.5
filelock==3.20.3
networkx==3.6.1
sympy==1.14.0
jinja2==3.1.6

# ---------- YOLO 推理所需(ultralytics 本地源码的依赖) ----------
ultralytics-thop==2.0.18

# ---------- 异常检测模块(DINOv3 导出/验证 ONNX) ----------
onnx==1.22.0
onnxruntime==1.29.0          # CPU 版，异常检测推理够用
# 若目标机有 NVIDIA GPU 且想让异常检测也走 GPU，可改用下面这行(需 CUDA 12.6 + cuDNN)：
# onnxruntime-gpu==1.29.0
